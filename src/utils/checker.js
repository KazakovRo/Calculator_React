const isOperator = value => value === '+' || value === '-' || value === '/' || value === 'x'

const isEqualBtn = value => value === '='

const isClearBtn = value => value === 'A/C'

export { isOperator, isEqualBtn, isClearBtn }
